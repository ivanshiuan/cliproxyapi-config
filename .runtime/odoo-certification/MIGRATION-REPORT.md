# MIGRATION-REPORT

Chain head: 938b0ab19f78 ← 8307a94acb62 ← 41772db92af3 (pre-existing head).

| Revision | Content | Evidence |
|---|---|---|
| 8307a94acb62 | suppliers, purchase_orders, purchase_order_lines (+indexes, FKs RESTRICT/CASCADE, tenant-scoped, UNIQUE (tenant, code)/(tenant, po_number)) | autogenerated with ZERO spurious drift; applied from zero on scratch DB; downgrade removes all 3 tables (3→0→3 round-trip); log raw-logs/migration-* |
| 938b0ab19f78 | +odoo_sync_attempts (int, server_default 0), +odoo_last_sync_error (text) on purchase_orders — dead-letter state | autogen detected exactly 2 columns; applied on dev + scratch; `alembic check` → "No new upgrade operations detected." on both |

No raw SQL in either migration. No data migration needed (new tables/columns only).
Rollback: `alembic downgrade 8307a94acb62` (drops dead-letter columns) /
`alembic downgrade 41772db92af3` (drops procurement tables) — both proven.
