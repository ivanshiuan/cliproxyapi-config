# DevSwarm + Restaurant API runtime dependencies
# Pinned to known-good major versions; minor/patch float for security updates.

# ─── DevSwarm core ──────────────────────────────────────────────────────
anthropic>=0.40.0,<1.0
langgraph>=0.2.50,<1.0
pydantic>=2.5,<3.0
python-dotenv>=1.0,<2.0
rich>=13.7,<15.0

# QA agent runs pytest on generated code; pinned here so sandbox has it.
pytest>=8.0,<9.0

# ─── Restaurant API (Phase 1) ───────────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.30,<1.0
alembic>=1.13,<2.0
pydantic-settings>=2.5,<3.0
greenlet>=3.0,<4.0
