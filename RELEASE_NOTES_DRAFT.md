# Release Notes Draft

> Auto-generated 2026-07-15 11:24 UTC — 132 commit(s) since first commit
> **Human review required before publishing.**

## ✨ Features
- Obsidian-ready vault + MOC + empire canvas + full skill routing table (`f9069e1`)
- empire second-brain — knowledge pipeline, digest skill, skill router (`a505977`)
- three-loop automation — Daily Brief, PR Babysitter, Changelog Drafter (`2eb0e7d`)
- Reviewer 回歸 eval 骨架 — 把 prompt 版本接成真正的回歸網 (`91bdddc`)
- Codex — 不變法則的單一真相源,Architect 與 Reviewer 共用 (`2efbfc5`)
- Hermes 對外通知層 — 蜂群終態主動推送 (`359b919`)
- 對抗式 Reviewer node — DevSwarm 升為 5-agent 蜂群 (`070f8f0`)
- LINE push on booking-confirmed and queue-called (`61d1142`)
- inbound webhook endpoint with signature verification (`94218e8`)
- SID 一鍵部署自動化 — deploy.sh 預檢 token/egress + make deploy + /deploy-sid skill (`d78e29d`)
- 接上真實 LINE 推播（HttpLineMessenger 實作 + 設定 + 驗證工具） (`b0d41f0`)
- voucher CSV export + campaign scheduling UI (starts_at/ends_at) (`8dfb1a5`)
- UIUX 全面升級 + PWA 圖示 + 100 輪復盤(14900/14900) (`120629b`)
- auto file-ingest — markitdown via isolated uv run, zero dep pollution (`3c3b0ca`)
- live prize-odds tuning in the 店長 console (`8ec6b13`)
- 店長 results dashboard for the wheel campaign (stats endpoint + panel) (`2da1a22`)
- gate 店長後台 + 櫃台端點 with shared-passcode session auth (`22a7de8`)
- 店長後台 UI (admin.html) — 活動/獎項/櫃台核銷 (`f353a09`)
- A4 戰術量化 + Memo 補全(集成/MC/歷史/戰術分) + 文件同步 (`74d4440`)
- A5 Ensemble 集成(模型+市場) + 100% 功能正確性驗證 (`68d28a9`)
- 大規模回測(8000場)+校準修復(temperature scaling γ=1.25) (`91f4ddf`)
- Path C 上線 — Worker proxy + App 設定頁 + 10輪復盤QA(1380斷言全綠) (`798c10d`)
- live 資料對接層 + 部署說明（三家API/自動降級/CORS proxy） (`1badc78`)
- Monte Carlo + 回測校準 + 歷史交手/大賽戰績庫 (`07c4172`)
- Phase 3.2b RFM 分眾 + 分眾推播 (`1d6a107`)
- Phase 3.2b RFM 分眾 + 分眾 LINE 推播 (`a154c7e`)
- match-intel skill — 賽事投研分析邏輯封裝為可呼叫 Skill (`422e5f6`)
- Sports Intelligence Desk — 世界盃賽事投研終端 (P0 全部) (`49b984a`)
- Phase 3.2c 連續回訪 Streak 倍率 (`d7db6f1`)
- Phase 3.2a 成效報表時間窗 (?days=N) (`552bd22`)
- Phase 3.1 成效報表 GET /membership/stats — 決策閉環 (`f047c11`)
- UGC 打卡/評論換獎 (人工審核發點) — 補洞 A (`e7dbabe`)
- 裂變飛輪 (Referral 雙邊推薦) — 補洞 C (`c34deae`)
- 儲值雙倍 (Stored Value 預付錢包) — 補洞 B (`8419abb`)
- 會員生命週期自動化引擎 (Phase 1 成長飛輪) (`752e09c`)
- 輪盤 QR Code 產生器 + 海報 + demo 頁品牌化 (`5176c51`)
- 輪盤抽獎前端 demo 頁 + 一鍵 seed (`53fde52`)
- 輪盤券夜間過期 job + 補 apscheduler 依賴 (`1982dd7`)
- 開幕引流輪盤抽獎引擎 (wheel-spin lottery campaign) (`3e921d2`)
- wire end-to-end points lifecycle (earn → expire) (`3f73ea5`)
- POST /customers/{id}/redeem — points redemption endpoint (`ce9791d`)
- /customers CRUD + points-history read (`eca6f0d`)
- close-the-loop — customer_id FK on Order + points + LINE (`07dac2a`)
- KDS poll + state transition endpoints (`81f4682`)
- /reservations + /queue routers with state-machine guards (`10cac81`)
- real Taiwan public-holiday lookup replaces weekend-only stub (`0cfa1ac`)
- 6 phase-1 calc engines + tooling + CI hardening (parallel swarm) (`852832c`)
- production hardening — observability, K8s health, jobs, deploy (`adf9e2d`)
- full project setup — CLAUDE.md + 5 commands + 3 agents + hooks (`8fc01d8`)
- end-to-end demo flow + seed data + commander handoff (`f524074`)
- 4 phase-1 routers — orders, stock, clock, events (parallel swarm) (`b900c4e`)
- http-layer foundation — DI, error envelope, test fixtures (`62f5ac2`)
- phase1 extension kit — KDS, reservation/queue, LINE, pyright, prompt versioning (`2fbbee9`)
- close 10 audit-identified loops (eng + F&B compliance) (`f6400ca`)
- alembic Make targets + real-DB smoke script (`00560e3`)
- Alembic initial migration + execution plan + backlog tooling (`71bc788`)
- Phase 1 backend scaffold — FastAPI + 18-table ORM (`e557c0c`)
- LangGraph agent swarm — PM/Architect/Coder/QA + self-heal (`7fb1040`)

## 🐛 Bug Fixes
- 用 datetime.UTC alias 修 ruff UP017（morning_brief / draft_changelog） (`020985b`)
- 把 workflow branch 從 main 改成實際主幹 [skip ci] (`d4d8d5d`)
- 處理 PR #13 review 回饋(對抗式審查 + 守衛強化) (`292e87f`)
- Claude 4.x/Fable models reject deprecated temperature param (`9e5a455`)
- 合併主幹後移除 LINE config 重複欄位（保留 #4 既有定義） (`e3a8628`)
- declare apscheduler dep + harden to_md.py CLI (`096127a`)
- esc() 一致性補齊 — 戰術/歷史區塊與設定頁輸入全轉義(深度復盤) (`b1cf377`)
- exclude uv-isolated to_md.py from pyright so full-check stays green (`50aea7f`)
- stylelint value-keyword-case — CSS 關鍵字小寫(optimizelegibility/currentcolor) (`e6db9cd`)
- PR#2 第二輪 CodeRabbit — 轉義傷病警報 + 文件可攜性 (`8996978`)
- 處理 PR#2 CodeRabbit 審查 — 安全/健全性/lint 修正 (`939ef94`)
- 對抗式復盤修真實缺陷 + 補傷病同步 + 交接文件(A+B) (`657faa9`)
- 並發鎖 + enum server_default 大小寫 (副盤檢查 round 7 — code-review) (`65a09f8`)
- 修復 orders.customer_id FK migration 無法 downgrade (副盤檢查 round 5) (`382d26c`)
- RFM 評分綁定 as_of 日期 (副盤檢查 round 3) (`7688ea1`)
- seed_growth_demo CI lint + engine dispose (`8f23213`)
- H/G 組賽前資料以查證事實校準（移除未驗證傷病） (`f0674e2`)
- 回應 CodeRabbit — 生命週期引擎正確性強化 (`75cf0bd`)
- 復盤修正 — 抽獎韌性、輪盤標籤對位、批次化與測試確定性 (`9a8fbf0`)

## ♻️ Refactor
- 回應 CodeRabbit review (`8f66cf5`)
- replace last stubs with calc-engine calls + minor fixes (`627e994`)
- swap BOM stubs for real consume_bom engine (`48ba44a`)
- swap classify_hours stub for real calc engine (`4015488`)

## ⚡ Performance
- streak 查詢僅在有基礎點數時執行 (副盤檢查 round 4) (`6a9eb3d`)

## 🧪 Tests
- 消除自有測試的 PytestWarning (副盤檢查 round 12-13) (`add3eb1`)
- 端到端串接測試 — spin(ref) 綁定 + close_order 達標 (`78330a7`)
- pytest suite for state, workspace, sandbox, imports (`10cb4ef`)

## 📝 Docs
- 把 4x 對抗式 Reviewer 編入 DevSwarm 架構（docs/02） (`46baa0c`)
- 新增 MASTER_DESIGN 完整主設計文件（可交付給其他 PM 審查/優化） (`9643699`)
- HANDOFF 第12行移除無法查證的精確條數，改述三輪審查全數 resolve（回應 CodeRabbit） (`117c64d`)
- add main() docstring in to_md.py (clears coverage warning) (`3bf756c`)
- 店長操作手冊（現場員工面 SOP） (`5c8ad4f`)
- HANDOFF 補上 2026-06-21 深度復盤紀錄與殘留風險 (`74ce26c`)
- set auto-open-PR as default workflow in project memory (`8681453`)
- wheel-campaign acceptance report (業務需求對照 + 實測證據) (`bd945e7`)
- document admin-console secrets + record wheel-campaign console work (`f9c9061`)
- 完整系統重建藍圖 SYSTEM_BLUEPRINT + 部署規格 BUILD_SPEC (`ef79c28`)
- 定案 Cloudflare Pages 專案名 498-win (498-win.pages.dev) (`0c112cc`)
- COMMANDER_HANDOFF 記錄本 session 獲客飛輪交付 (`5a7484d`)
- 記錄 enum server_default 全庫技術債 (副盤檢查 round 7 衍生) (`8c64851`)
- 14_store_ops_playbook 店長營運 SOP 手冊 (`64d13f4`)
- 賽事投研系統 Master Plan（11資料庫/13模型/4階段路線圖/校準機制） (`11d4451`)
- 加入顧問診斷 §8 獲客補洞藍圖 (`d2935b1`)
- 補齊輪盤抽獎模組 docstring (CRUD/路由/schema/helper) (`1b3fc47`)
- update commander handoff with autonomous-mode session log (`6063513`)
- tighten discount_resolver + labor_hours_classifier prose (`475233c`)
- cogs_variance_detector + labor_hours_classifier briefs (`5af0f84`)
- bom_consumer + discount_resolver pure-fn briefs (`776464c`)
- 4 router specs for Phase 1 FastAPI endpoints (`04ae570`)
- DevSwarm operator runbook (07) (`fea06cf`)
- full project planning artifacts (`c50de45`)

## 🔧 Chores
- daily brief 2026-07-15 [skip ci] (`4fba0de`)
- daily brief 2026-07-14 [skip ci] (`f4d74ec`)
- daily brief 2026-07-13 [skip ci] (`7b309f2`)
- daily brief 2026-07-10 [skip ci] (`22799c1`)
- daily brief 2026-07-09 [skip ci] (`a8e350a`)
- update changelog draft [skip ci] (`2a1491a`)
- daily brief 2026-07-08 [skip ci] (`91fdc77`)
- add BrowserAct skill, setup script, and setup docs (`07da0b0`)
- daily brief 2026-07-07 [skip ci] (`dc7eccf`)
- daily brief 2026-07-06 [skip ci] (`1c1dac2`)
- update changelog draft [skip ci] (`67eae05`)
- daily brief 2026-07-03 [skip ci] (`94ba11a`)
- daily brief 2026-07-02 [skip ci] (`419b673`)
- daily brief 2026-07-01 [skip ci] (`3782471`)
- PreToolUse 阻擋 hook — 把帝國規則從祈禱變成牆 (`68ffbfe`)
- 前端打包腳本 + Cloudflare Pages 獨立部署套件 (`fe9fcbc`)
- add pytest-cov + make coverage / coverage-quick targets (`27dae0e`)
- CI workflow, ruff cleanup, morning brief, second demo spec (`22c574c`)

## 📦 Other
- TIGER LINE PRIME V3.0 — full CLI + Cloudflare & GitHub Pages web deploy (`298173f`)
- ci: GitHub Pages 自動部署 SID（configure-pages 自動啟用 + build + deploy） (`e5b0c8a`)
- test+fix: HTTP 整合測試 + 修兩個更深的 BUG (副盤檢查 round 10-11) (`a8dd147`)
- docs+fix: update execution plan + tiebreaker on points ledger sort (`96a89b1`)
- test+chore: mocked end-to-end graph tests + Makefile (`80d0fc9`)

---
*Draft by `changelog-drafter` workflow — edit before publishing.*