# STAGING-ACCEPTANCE-CHECKLIST

**Purpose:** Gate before declaring staging READY and before any production
authorization discussion.

**Required by:** Ivan sign-off on all items checked.

---

## Section A: Infrastructure (7 checks)

| # | Check | Method | Pass Criteria |
|---|---|---|---|
| A1 | Odoo web UI accessible via SSH tunnel | SSH + browser | Login successful |
| A2 | Odoo version confirmed | Settings -> About | `17.0-20260723` |
| A3 | PostgreSQL version confirmed | `docker exec db psql -U odoo_staging -c "SELECT version()"` | PostgreSQL 16.x |
| A4 | Odoo port NOT exposed to internet | `curl http://<public-ip>:18069` from external machine | Connection refused |
| A5 | HTTPS on restaurant_api endpoint | `curl https://staging.api.example.com/health/ready` | `{"status":"ready"}` |
| A6 | Odoo image digest matches evidence | `docker inspect odoo-staging --format '{{.Image}}'` | matches `sha256:f836...` |
| A7 | PG image digest matches evidence | `docker inspect odoo-staging-db --format '{{.Image}}'` | matches `sha256:33f9...` |

---

## Section B: Service Account Rights (5 checks)

| # | Check | Method | Pass Criteria |
|---|---|---|---|
| B1 | Service account is NOT admin | `res.users.read([svc_id], ["groups_id"])` via admin RPC | `base.group_system` NOT in groups |
| B2 | Service account HAS Accounting | Group check | `account.group_account_user` present |
| B3 | Service account HAS Contact Creation | Group check | `base.group_partner_manager` present |
| B4 | Service account API key works | `HttpOdooClient.from_env().identity()` | Returns username, not error |
| B5 | API key never appears in logs | `grep -r "odoo_api_key" /var/log/` | No matches |

---

## Section C: Chart of Accounts (5 checks)

| # | Check | Method | Pass Criteria |
|---|---|---|---|
| C1 | Account 1310 存貨 exists | `account.account.search([["code","=","1310"]])` | Returns 1 result |
| C2 | Account 1360 進項稅額 exists | `account.account.search([["code","=","1360"]])` | Returns 1 result |
| C3 | Account 2100 應付帳款 exists | `account.account.search([["code","=","2100"]])` | Returns 1 result |
| C4 | Journal PUR exists | `account.journal.search([["code","=","PUR"]])` | Returns 1 result |
| C5 | Company default payable = 2100 | `res.company.read([1], ["property_account_payable_id"])` | Maps to 2100 |

---

## Section D: Happy Path Vendor Bill (10 checks)

| # | Check | Method | Pass Criteria |
|---|---|---|---|
| D1 | PO sync creates vendor bill | `sync_purchase_orders(tenant_id=...)` | `result.pushed == 1` |
| D2 | Vendor bill is in_invoice | `account.move.read([move_id], ["move_type"])` | `in_invoice` |
| D3 | State is draft | `account.move.read([move_id], ["state"])` | `draft` |
| D4 | Partner bound correctly | `move["partner_id"][0] == supplier_partner_id` | True |
| D5 | Invoice lines use 1310 and 1360 | Read `invoice_line_ids` accounts | Only 1310 and 1360 |
| D6 | Payable generated by Odoo | Line with `display_type=payment_term`, account 2100 | Present |
| D7 | Move is balanced | `total_debit == total_credit > 0` | True |
| D8 | Payable == PO total | `payable_credit == po.total` | True (TWD, Decimal) |
| D9 | Source marker present | `move["ref"]` contains `[src:purchase_order:<tid>:<pid>]` | True |
| D10 | Invoice date set | `move["invoice_date"]` | Not null, = PO received_at |

---

## Section E: Idempotency (3 checks)

| # | Check | Method | Pass Criteria |
|---|---|---|---|
| E1 | Plain rerun creates zero new bills | `sync_purchase_orders` again | `result.pushed == 0` |
| E2 | Stamp-loss replay recovers same bill | Clear `odoo_move_id`, re-sync | Same `move_id` returned |
| E3 | Stamp-loss creates zero new bills | Verify marker count | `search_count([["ref","like","[src:...]"]]) == 1` |

---

## Section F: Negative Cases (3 checks)

| # | Check | Method | Pass Criteria |
|---|---|---|---|
| F1 | Non-TWD PO fails cleanly | `sync_purchase_orders` with USD PO | `result.failed == 1`, no Odoo move |
| F2 | Zero-amount PO skipped | `sync_purchase_orders` with 0-total PO | `result.skipped == 1` |
| F3 | Missing supplier PO fails cleanly | Soft-deleted supplier | `result.failed == 1`, no Odoo move |

---

## Section G: Permission Contract (5 checks)

| # | Check | Method | Pass Criteria |
|---|---|---|---|
| G1 | `res.users.read` blocked | Attempt via `svc` account | `ModelNotAllowedError`, zero egress |
| G2 | `account.move.unlink` blocked | Attempt via `svc` account | `OperationNotAllowedError`, zero egress |
| G3 | `action_register_payment` blocked | Attempt via `svc` account | `OperationNotAllowedError`, zero egress |
| G4 | Auto-post refused | `post_journal_entry(je, post=True)` | `PostingNotPermittedError` |
| G5 | No bill posted to non-PUR journal | Attempt with disallowed journal code | `OperationNotAllowedError` |

---

## Section H: make full-check (4 checks)

| # | Check | Method | Pass Criteria |
|---|---|---|---|
| H1 | ruff lint | `make full-check` | 0 errors |
| H2 | pyright type check | `make full-check` | 0 errors (basic mode) |
| H3 | pytest (all 106+ tests) | `make full-check` | 0 failures |
| H4 | Alembic heads check | `make full-check` | Single head, no pending |

---

## Section I: Backup Restore Drill (4 checks)

| # | Check | Method | Pass Criteria |
|---|---|---|---|
| I1 | Backup exists | `ls /opt/backups/YYYYMMDD/` | `.pgdump` files present |
| I2 | Restore completes without error | Run restore procedure (06_STAGING-BACKUP-RESTORE.md) | Exit code 0 |
| I3 | Post-restore sync works | Create new PO, sync | New vendor bill created |
| I4 | make full-check post-restore | `make full-check` | All green |

---

## ACCEPTANCE VERDICT

All 46 items in Sections A-I must be checked before staging is declared
READY.

- **Current status:** NOT YET EXECUTED (deployment plan phase)
- **Required sign-off:** Ivan
